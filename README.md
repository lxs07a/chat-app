1. Make sure [NodeJS](https://nodejs.org/) and [npm](https://www.npmjs.com/) are installed.

2. Install dependencies
    
    ```
   npm install
    ```

3. Start your app
    
    ```
    npm start
    ```